# 已知不处理问题

本文件记录当前明确接受的已知限制。除非用户明确要求重新评估，否则开发、排查、测试和代码审查无需处理以下问题。

## 连接替换后序列号基线丢失

`CozyLifeDevice.replace_client()` 替换传输控制协议（Transmission Control Protocol，TCP）客户端后，`last_state_sequence_number` 会改为读取新客户端记录的 Unix 毫秒时间戳序列号（sn），不会继承旧客户端已经接受的最大值。

已确认的复现场景：实际传输客户端先接收序列号 `200`，替换连接后，新客户端仍会接收并转发更旧的序列号 `150`。

根因位于 `custom_components/hass_cozylife_local_pull/device.py:60` 和 `custom_components/hass_cozylife_local_pull/device.py:69`：前者只读取当前客户端的序列号，后者替换客户端时没有保留旧连接的序列号基线。

当前暂不处理，原因如下：

- 所有序列号都是 Unix 毫秒时间戳，正常情况下新连接的首个查询结果应比旧连接更新。
- 当前不考虑设备时钟回拨、跨连接延迟上报和连接替换过程中的并发问题。
- 该情况不会导致连接失败；只有新客户端收到更小序列号时，旧状态才可能暂时覆盖当前状态，后续更新上报会恢复正确状态。

只有实际观察到连接替换后的状态回退，或需求改为保证同一物理设备整个生命周期内序列号严格递增时，才重新评估该问题。
